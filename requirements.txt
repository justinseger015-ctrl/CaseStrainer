# CaseStrainer dependencies (updated for Python 3.13, Windows, and pip freeze)

# Core dependencies
six==1.17.0
python-dateutil==2.9.0.post0
lxml==5.4.0

# Web framework
Flask==3.1.1
flask-cors==6.0.0
Flask-Session==0.8.0
# flask-restful (optional, not required by pip freeze)
Werkzeug==3.1.3

# Scientific computing
numpy==2.2.6
scikit-learn==1.6.1
scipy==1.15.3

# Natural language processing
nltk  # Add version if needed

# For API requests
requests==2.32.3
typing_extensions==4.13.2
urllib3==2.4.0

# For parsing HTML
beautifulsoup4==4.13.4

# For handling PDF files
PyPDF2==3.0.1
pdfminer.six==20250506

# For environment variable management
python-dotenv>=1.1.0

# For legal citation extraction
eyecite  # Add version if needed
pyahocorasick  # Add version if needed

# For Word document processing
python-docx  # Add version if needed

# For OpenAI API integration
openai==0.15.0

# For SSL/HTTPS support
pyopenssl==20.0.1

# For production deployment
waitress==3.0.2  # Production WSGI server
gunicorn==20.1.0

# For templates and security
Jinja2==3.1.6
MarkupSafe==3.0.2

# Other useful packages
# pre_commit, virtualenv, wheel, etc. for dev only
# itsdangerous version pin removed; Flask 3.1.1 requires itsdangerous>=2.2.0
itsdangerous  # Use latest compatible version
sentry-sdk==2.1.1
bandit==1.7.7

# For Vue.js integration
importlib-metadata==3.10.0  # For dynamic module loading


# Type hints
typing==3.7.4.3
# gevent removed: not compatible with Python 3.13; Waitress is used as the production WSGI server
