services:
  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: casestrainer-nginx-prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx-ssl-fixed.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./casestrainer-vue-new/dist:/usr/share/nginx/html/casestrainer
    restart: unless-stopped
    networks:
      - default

networks:
  default:
    external: true
    name: casestrainer_default
