"""
Backend Logging Utility Module

This module provides logging utilities for the CaseStrainer backend.
Currently a placeholder for future logging enhancements.
"""

import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

def setup_logging():
    """Setup logging configuration for the backend."""
    logger.info("Backend logging utility initialized")
    return logger 